```pnpm install vue ```
Running this command(命令) will add the dependency（依赖） to the workspace root, which（那些） might not be what you want - if you really meant it, make it explicit（明确的） by running this command again with the -w flag (or --workspace-root). If you don't want to see this warning anymore, you may set the ignore-workspace-root-check setting to true.

make by 通过……方式制作；被……制造

deprecated [ˈdeprəkeɪtɪd]强烈反对

This modules directory目录 was created using a different public-hoist [hɔɪst]吊起，提升-pattern [ˈpæt(ə)n]模式，方式； value. Run "pnpm install" to recreate the modules directory.


scheduler  [ˈskedʒuːlər] n. 计划员；时间调度员；程序机，调度机；调度程序；制表人

flush [flʌʃ]；冲洗 齐头排的